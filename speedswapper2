! Swap caps lock and control (xinit then uses xcape to allow "caps lock"
! (now control) to function as both a control key (when held and another 
! button pressed), or as escape (when pressed without another key.
remove Lock = Caps_Lock
keysym Caps_Lock = Control_L
add Control = Control_L
